import React from 'react';
import VirtualHeroBackground from './VirtualHeroBackground';

export default function TechHeroBackground({ virtualMode = 'cyber-mesh' }) {
  return <VirtualHeroBackground virtualMode={virtualMode} />;
}
