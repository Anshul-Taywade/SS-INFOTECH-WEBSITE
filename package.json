{
  "name": "ss-infotech-mern-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend && npm run build --prefix admin",
    "start": "npm run build && node backend/server.js",
    "dev": "npm run build && node backend/server.js",
    "dev:multi": "concurrently -n \"BACKEND,FRONTEND,ADMIN\" -c \"cyan,magenta,yellow\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix admin\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}